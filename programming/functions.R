# Syntax einer Funktion
estimate_bmi <- function(height_cm, weight_kg) {
  
  ###
  tmp <- weight_kg / (0.01*height_cm)^2
  return(tmp)
}

# BMI Henrik
#bmi_henrik <- estimate_bmi(height_cm=180, weight_kg=75)

estimate_bmi(height_m = )

hist(c(1, 2, 2, 5), "scott")



### Funktion in der Datenanlyse --------------------

